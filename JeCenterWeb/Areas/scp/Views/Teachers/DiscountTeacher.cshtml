@model IEnumerable<JeCenterWeb.Models.StudentDiscount>

@{
    ViewData["Title"] = "Teachers";
}
<div>
<a class="btn btn-outline-info" asp-action="Details" asp-route-id="@ViewBag.TeacherId">
        @ViewBag.TeacherName
</a>
</div>
<br />
@* <form asp-action="AddDiscount" method="post">
    <div class="row">
        <input type="hidden" name="TeacherId" value="@ViewBag.TeacherId" />
        <div class="col-4">
            <div class="form-group">
                <select name="StudentId" class="form-control select2" asp-items="ViewBag.StudentId">
                    <option value="0">أختار  الطالب</option>
                </select>
            </div>
        </div>
         <div class="col-2">
            <div class="form-group">
                <select name="LectureType" class="form-control" >
                    <option value="0">مجموعة </option>
                    <option value="1">مراجعة</option>
                    <option value="2">أمتحان</option>
                </select>
            </div>
        </div>
         <div class="col-2">
            <div class="form-group">
                <select name="physicalyearId" class="form-control " asp-items="ViewBag.physicalyearId">
                    <option value="0">السنة الدراسية</option>
                </select>
            </div>
        </div>
        <div class="col-2">
            <div class="form-group">
                <input name="TeacherDiscount" class="form-control" placeholder="خ مدرس" />
            </div>
        </div>
        <div class="col-2">
            <div class="form-group">
                <input name="CenterDiscount" class="form-control" placeholder="خ سنتر" />
            </div>
        </div>
       
    </div>
    <div class="row">
        <div class="col-8">
            <div class="form-group">
                <input name="Notes" class="form-control text-center" placeholder=" سبب الخصم  " value=" خصم " />
            </div>
        </div>
        <div class="col-4">
            <input type="submit" value="إضافة" class="btn btn-info btn-block" />
        </div>
    </div>
  
</form> *@

<table class="table table-striped table-sm text-center">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.teacherDiscount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CenterDiscount)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.FinancialAccount.AccountName
                </td>
                <td>
                    @item.Discount
                </td>
                <td>
                    @item.teacherDiscount
                </td>
                <td>
                    @item.CenterDiscount
                </td>
            </tr>
        }
    </tbody>
</table>