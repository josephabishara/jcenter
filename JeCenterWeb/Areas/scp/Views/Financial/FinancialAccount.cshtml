@model IEnumerable<JeCenterWeb.Models.ViewModel.ExpensesBalance>

@{
    ViewData["Title"] = "Index";
}
@* <form asp-action="CreateFinancialAccount" method="post">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <input name="AccountName" class="form-control" />
            </div>
        </div>
        <div class="col-md-2">
            <input type="hidden" name="" />
            <input type="submit" value="حفظ" class="btn btn-info btn-block" />
        </div>
    </div>
</form>
 *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Balance)
            </th>
           
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a class="btn btn-block" asp-action="Details" asp-route-id="@item.Id">
                        @item.FullName
                    </a>
                </td>
                <td>
                    <a class="btn btn-block" asp-action="Details" asp-route-id="@item.Id">
                        @item.Balance
                    </a>
                </td>
               
            </tr>
        }
    </tbody>
</table>
